stages:
  - security
  - quality
  - build
  - smoke
  - release
  - pages

include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'
  AST_ENABLE_MR_PIPELINES: 'true'
  GIT_DEPTH: 1
  UV_CACHE_DIR: .uv-cache
  # Pinned toolchain. Every change here is an MR — no out-of-band rotation via
  # CI/CD variables. Images are digest-pinned; tooling tarballs carry both a
  # tarball hash (checked on download) and an extracted-binary hash (checked on
  # every run, so a poisoned cache cannot bypass integrity).
  # uv 0.11.7-python3.14-trixie
  UV_IMAGE: ghcr.io/astral-sh/uv@sha256:9e9b645dc1a26a9e10ec64073c37e05d85b0d41c2b6946603c8863792f38cade
  # osv-scanner 2.3.5
  OSV_SCANNER_IMAGE: ghcr.io/google/osv-scanner@sha256:4d3d1a42ba435ac706286fec518c044f049c9753d21260b5bae60bab8740ed97
  # alpine 3.20.10
  ALPINE_IMAGE: alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc
  # semgrep-sast 6.18.0 (bundles semgrep 1.145.0 on python 3.12.13)
  SEMGREP_IMAGE: registry.gitlab.com/security-products/semgrep@sha256:d3aecff03d2a7a37f9ebfa396cf25499491e27c31d2ca75030f00b11337de5fe
  BUN_VERSION: "1.3.12"
  BUN_SHA256: "11dc3ee11bc1695e149737c6ca3d5619302cf4346e6b8a6ec7988967ef01ddc5"
  BUN_BIN_SHA256: "92a1cd8b6185f676010bb18e767dfc65c772273e79ae9984480843261939eeaa"

# --- Top-level workflow rules ---
# Four-lane pipeline model:
#   1. Merge request  → full CI (quality, golden, security, dependency-scan)
#   2. Push to main   → quality (for coverage badge) → pages deploy
#   3. Tag push       → release (version-check, build, smoke-wheel, publish)
#                       plus dependency-scan rerun for fresh CVE disclosures
#   4. Schedule       → weekly dependency-scan rerun (CVE disclosures against
#                       unchanged lockfiles) + outdated-report (tooling drift).
#                       Both jobs allow_failure: true on this lane — the schedule
#                       pipeline is informational (review the log manually), not
#                       a gate. Schedule itself is configured in the GitLab UI
#                       (Settings → CI/CD → Schedules).
# Feature-branch pushes produce no pipeline — the MR is the gate.
workflow:
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "schedule"

# --- Security ---

# The top-level `sast` is a scriptless group job from the template.
# Never override its rules or script — only pin its stage (template default
# is `test`, which we don't define). Scope the actual analyzer below.
sast:
  stage: security

# TODO(dagshund-3er0): the pinned analyzer (semgrep 1.145.0, GitLab 6.18.0) parses
# stale Python and TypeScript grammars — PEP 695 `type X = Y` and PEP 634 `match`
# statements in src/**, plus `/<!--/g` in js/src/html-assembler.ts, all parse-fail,
# and parse-failed files are silently skipped with zero rule coverage. As of
# 2026-04-17 every `:6.x` tag resolves to the digest above — no newer :6 analyzer
# exists upstream. Re-probe when GitLab bumps the SAST template default or ships
# analyzer 7; bump SEMGREP_IMAGE and close the bead when all 6 files parse clean.
semgrep-sast:
  stage: security
  image:
    name: ${SEMGREP_IMAGE}
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

secret_detection:
  stage: security
  interruptible: true
  allow_failure: false
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# --- Dependency vulnerability scan ---
# OSV-Scanner against uv.lock + js/bun.lock. Hard-fails on known CVEs.

dependency-scan:
  stage: security
  image:
    name: ${OSV_SCANNER_IMAGE}
    entrypoint: [""]
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    # Defense in depth: rescan on tag because CVE disclosures can land against
    # unchanged lockfiles between the last MR and the release cut.
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    # Weekly schedule: catch CVE disclosures landing against unchanged
    # lockfiles between releases. Informational — does not fail the scheduled
    # pipeline (review the log manually). MR/tag behavior above stays strict.
    - if: $CI_PIPELINE_SOURCE == "schedule"
      allow_failure: true
  interruptible: true
  allow_failure: false
  script:
    # Binary lives at /osv-scanner (not on PATH).
    - /osv-scanner --lockfile=uv.lock --lockfile=js/bun.lock

# --- Outdated-tooling report (schedule only) ---
# Lists outdated Python + JS packages once a week. Informational: the pipeline
# stays green regardless of output, and image digest pins (UV_IMAGE, etc.) are
# tracked separately under periodic audit (see dagshund-2omy).

outdated-report:
  extends: .release-setup
  stage: security
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  allow_failure: true
  script:
    - echo "--- Python (uv) ---"
    - uv pip list --outdated
    - echo "--- JavaScript (bun) ---"
    - bun outdated --cwd js

# --- Shared setup ---
# Downloads bun, syncs uv, installs js deps. Release jobs extending this
# must override `interruptible: false`.

.release-setup:
  image: ${UV_IMAGE}
  interruptible: true
  cache:
    - key:
        files:
          - uv.lock
          - js/bun.lock
        # Namespace cache by ref protection so MR/feature branches can't
        # poison the cache entries consumed by protected refs (main, tags).
        prefix: ${CI_COMMIT_REF_PROTECTED}
      paths:
        - ${UV_CACHE_DIR}/
        - js/node_modules/
    # Bun binary cache, keyed on BUN_VERSION.
    - key: "bun-${BUN_VERSION}-${CI_COMMIT_REF_PROTECTED}"
      paths:
        - .cache/bun/
  before_script:
    - |
      if [ ! -x .cache/bun/bun ]; then
        mkdir -p .cache/bun
        curl -fsSL --max-time 120 -o /tmp/bun.zip "https://github.com/oven-sh/bun/releases/download/bun-v${BUN_VERSION}/bun-linux-x64.zip"
        echo "${BUN_SHA256}  /tmp/bun.zip" | sha256sum --check
        unzip -qo /tmp/bun.zip -d /tmp
        mv /tmp/bun-linux-x64/bun .cache/bun/bun
        rm -rf /tmp/bun.zip /tmp/bun-linux-x64
      fi
    # Verify the binary on every run — a poisoned cache must not slip past.
    - echo "${BUN_BIN_SHA256}  .cache/bun/bun" | sha256sum --check
    - install -m 0755 .cache/bun/bun /usr/local/bin/bun
    - ln -sf bun /usr/local/bin/bunx
    - bun --version
    - uv --version
    - uv sync --locked
    - bun install --frozen-lockfile --cwd js

# --- Quality gate (every pipeline) ---

quality:
  extends: .release-setup
  stage: quality
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
    # Main push: feeds the coverage badge and gates `pages` below.
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
  script:
    - bun run --cwd js biome check src/ tests/
    - uv run ruff check src/ tests/ fixtures/tooling/
    - bun run --cwd js tsc --noEmit
    - uv run ty check src/ tests/ fixtures/tooling/
    - bun test --cwd js --coverage --coverage-reporter=text
    - uv run pytest --cov=dagshund --cov-report=term-missing
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'

# --- Version check (tag push only) ---

version-check:
  image: ${ALPINE_IMAGE}
  stage: quality
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
  script:
    - |
      PKG_VERSION=$(sed -n 's/^__version__ = "\(.*\)"/\1/p' src/dagshund/__init__.py)
      TAG_VERSION=${CI_COMMIT_TAG#v}
      if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
        echo "ERROR: Package version ($PKG_VERSION) does not match tag ($TAG_VERSION)"
        exit 1
      fi

# --- Build wheel (tag push only) ---
# Release job: interruptible: false, never cancel an in-flight build.

build:
  extends: .release-setup
  stage: build
  interruptible: false
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
  needs:
    - quality
    - version-check  # cheapest possible gate against publishing a mismatched wheel — never make optional
  script:
    - bun run --cwd js build:template
    - cp plugins/dagshund/skills/dagshund/SKILL.md src/dagshund/_assets/SKILL.md
    - uv build
    - sha256sum dist/* > dist/SHA256SUMS
  artifacts:
    paths:
      - dist/
    expire_in: 1 hour

# --- Golden fixture checks (MR only) ---
# Diffs text + markdown + exit codes + graph structure against stored expected
# files. The graph extractor runs under Bun, so this job needs the full setup.
# On tag pipelines `smoke-wheel` re-runs the golden check against the installed
# wheel, so `golden` itself is MR-only.

golden:
  extends: .release-setup
  stage: smoke
  variables:
    NO_COLOR: "1"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  needs:
    - quality
  allow_failure: false
  script:
    - ./fixtures/tooling/generate_expected.sh --check

# --- Wheel smoke (tag push only) ---
# Installs the built wheel into a fresh venv, runs the golden checks against
# the installed binary, and verifies HTML output baked into the wheel.
# Extends .release-setup because the graph extractor needs bun + js deps.

smoke-wheel:
  extends: .release-setup
  stage: smoke
  interruptible: false
  variables:
    NO_COLOR: "1"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
  needs:
    - quality
    - build
  allow_failure: false
  script:
    - uv venv /tmp/smoke-env
    - uv pip install --python /tmp/smoke-env/bin/python dist/*.whl
    - export PATH="/tmp/smoke-env/bin:$PATH"
    # Absolute path so this doesn't silently fall back to `uv run dagshund`
    # if the PATH export ever gets reordered away from the invocation.
    - DAGSHUND=/tmp/smoke-env/bin/dagshund ./fixtures/tooling/generate_expected.sh --check
    # Installed-binary entry-point smoke (string-compare avoids SIGPIPE from grep -q)
    - '[[ "$(dagshund --version)" == *dagshund* ]]'
    - 'if dagshund fixtures/golden/broken-json.json >/dev/null 2>&1; then echo "ERROR: broken-json.json should have failed"; exit 1; fi'
    # HTML output (requires template baked into the wheel)
    - dagshund fixtures/golden/mixed-changes/plan.json -o /tmp/smoke-out.html
    - test -s /tmp/smoke-out.html
    - grep -q "<html" /tmp/smoke-out.html

# TODO(dagshund-2ge9): adopt PyPI attestations (PEP 740) once GitLab CI has a
# blessed path. As of 2026-04, producing attestations is GitHub-Actions-first;
# pypi-attestations is "primarily for experimentation" and the PyPI docs open
# their manual section with "STOP!". Do not hand-roll sigstore-python here.
# Watch pypi-attestations + https://docs.pypi.org/attestations/producing-attestations/
# for a first-class GitLab flow, then wire a SIGSTORE_ID_TOKEN id_tokens block
# into the build job and sign dist/*.whl before publish.
publish-test:
  stage: release
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: manual
  image: ${UV_IMAGE}
  environment: release-test
  needs:
    - build
    - smoke-wheel
  id_tokens:
    TESTPYPI_ID_TOKEN:
      aud: testpypi
  script:
    - sha256sum --check dist/SHA256SUMS
    - uv publish --publish-url https://test.pypi.org/legacy/

publish:
  stage: release
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: manual
  image: ${UV_IMAGE}
  environment: release
  needs:
    - build
    - smoke-wheel  # explicit — defends against future edits to publish-test's needs
    - publish-test
  id_tokens:
    PYPI_ID_TOKEN:
      aud: pypi
  script:
    - sha256sum --check dist/SHA256SUMS
    - uv publish

# --- GitLab Pages demo site (every push to main) ---
# Relies on `main` branch protection requiring pipelines to succeed, so every
# commit landing here has already passed MR validation. If that setting ever
# drifts off, a broken main could ship to Pages.

pages:
  extends: .release-setup
  stage: pages
  needs:
    - quality
  rules:
    # Only re-deploy when something that affects the rendered site changed.
    # js/**          → bun-built template
    # src/dagshund/** → Python CLI that embeds the template + writes HTML
    # fixtures/golden/** → plans rendered into public/*.html
    # docs/pages/**  → static landing page
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
      changes:
        paths:
          - 'js/**/*'
          - 'src/dagshund/**/*'
          - 'fixtures/golden/**/*'
          - 'docs/pages/**/*'
  script:
    - bun run --cwd js build:template
    - mkdir -p public
    - cp docs/pages/index.html public/index.html
    - |
      for dir in fixtures/golden/*/; do
        name=$(basename "$dir")
        if [[ -f "$dir/plan.json" ]]; then
          uv run dagshund -q "$dir/plan.json" -o "public/$name.html"
        fi
      done
  artifacts:
    paths:
      - public
