[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "etl_lib"
version = "0.1.0"
description = "Golden-fixture ETL helper wheel (content is irrelevant, only the version bump matters)"
requires-python = ">=3.10"

[project.scripts]
etl_run = "etl_lib:main"
